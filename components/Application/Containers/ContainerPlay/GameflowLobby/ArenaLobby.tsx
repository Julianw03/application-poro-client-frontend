import {LobbyState} from '../../../../../types/Store';

export interface ArenaLobbyProps {
    lobby: LobbyState | null;
}

export default function ArenaLobby({lobby}) {

    return (
        <></>
    );
}